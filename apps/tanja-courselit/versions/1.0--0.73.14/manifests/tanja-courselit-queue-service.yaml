apiVersion: v1
kind: Service
metadata:
  name: tanja-courselit-queue
  labels:
    app: tanja-courselit-queue
    app.kubernetes.io/name: tanja-courselit
spec:
  selector:
    app: tanja-courselit-queue
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
